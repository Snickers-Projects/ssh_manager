<Window x:Class="SshManager.Views.PasswordPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter Password"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FocusManager.FocusedElement="{Binding ElementName=PasswordInput}">

    <StackPanel Margin="16">
        <TextBlock x:Name="PromptText" TextWrapping="Wrap" Margin="0,0,0,12"/>
        <PasswordBox x:Name="PasswordInput" Padding="4" Margin="0,0,0,16"
                     KeyDown="PasswordInput_KeyDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Connect" Padding="20,6" Margin="0,0,8,0" IsDefault="True"
                    Click="Connect_Click"/>
            <Button Content="Cancel" Padding="20,6" IsCancel="True"
                    Click="Cancel_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
