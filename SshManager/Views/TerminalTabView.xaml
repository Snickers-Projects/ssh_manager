<UserControl x:Class="SshManager.Views.TerminalTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Loaded="UserControl_Loaded">

    <DockPanel>
        <!-- Status bar -->
        <Border DockPanel.Dock="Bottom" Background="#2D2D2D" Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusText}"
                           Foreground="#AAAAAA" FontSize="11" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="Reconnect" Padding="8,2" Margin="4,0"
                        FontSize="11" Command="{Binding ReconnectCommand}"/>
                <Button Grid.Column="2" Content="Disconnect" Padding="8,2"
                        FontSize="11" Command="{Binding DisconnectCommand}"/>
            </Grid>
        </Border>

        <!-- xterm.js terminal via WebView2 -->
        <wv2:WebView2 x:Name="TerminalWebView"
                       DefaultBackgroundColor="#1E1E1E"/>
    </DockPanel>
</UserControl>
